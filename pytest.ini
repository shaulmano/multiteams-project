[pytest]
testpaths = tests
markers =
    smoke: Basic sanity — app is up
    api: Pure API contract tests (no browser)
    ui: Browser tests via Playwright
    crud: Create / update / delete flows
addopts = -v --tb=short --html=test-report.html --self-contained-html
